[package]
name = "projection"
version = "0.1.0"
edition = "2021"

[dependencies]
ccync-foundation = { path = "../ccync-foundation" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
toml = "0.8"

[dev-dependencies]
tempfile = "3"
